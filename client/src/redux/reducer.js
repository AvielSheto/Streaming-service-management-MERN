function reducer(
    state = {
        users: [
            {id: "", name: "Aviel Sheto"}
        ],
    },
    action
){
    switch (action.type){

        default:
            return state;
    }
}

export default reducer;